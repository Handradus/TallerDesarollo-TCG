This script generates a Word document `docs/VAN_CLP_calculado.docx` with the VAN_CLP calculations.

Requirements:
- Python 3.x
- python-docx library

Run:

```bash
pip install python-docx
python scripts/generate_van_docx.py
```

The generated file will be saved at `docs/VAN_CLP_calculado.docx`.
